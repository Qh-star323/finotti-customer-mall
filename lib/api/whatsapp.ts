// =============================================================
// INTEGRATION SEAM — WhatsApp Business Platform
// Status: STUB (using wa.me links for now)
// To activate:
//   1. Set WA_TOKEN and WA_PHONE_NUMBER_ID in .env.local
//   2. npm install (no additional package needed — use native fetch)
//   3. Implement send() using:
//      POST https://graph.facebook.com/v19.0/{phoneNumberId}/messages
//      Authorization: Bearer {WA_TOKEN}
//   4. Replace buildWhatsAppURL() calls with API send() calls
// =============================================================

export const whatsappApi = null
