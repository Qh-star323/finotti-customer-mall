// =============================================================
// INTEGRATION SEAM — Resend Email
// Status: STUB (no emails sent in mock mode)
// To activate:
//   1. Set RESEND_API_KEY in .env.local
//   2. npm install resend
//   3. Create reusable email templates in emails/ directory
//   4. Implement: sendOrderConfirmation(), sendDeliveryUpdate(), sendAfterSalesUpdate()
//   5. Call from Server Actions (app/actions/) — never from client
// =============================================================

export const emailApi = null
